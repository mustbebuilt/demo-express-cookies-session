# Demo Express Cookies Session

This is a basic Express app for demoing cookie-session middleware.

npm install cookie-session

Assuming Node and Nodemon installed to set up run:

npm install
nodemon app
